feat: implement Sponsor Card system + fix round-robin regressions

## Sponsor Card System (primary task)
- shared/schema.ts: add tournament_sponsors table + sponsorPagesEnabled bool
- server/db.ts: CREATE TABLE IF NOT EXISTS migration for sponsor tables
- server/storage.ts: full CRUD for sponsors + setSponsorPagesEnabled
- server/routes.ts: 6 sponsor API endpoints (GET/POST/PUT/DELETE/PATCH/reorder)
- SponsorCard.tsx: unskippable 5s countdown card with logo, name, badge, blurb
- SponsorCardFlow.tsx: fixed inset-0 z-[100] overlay, fade transitions
- SponsorSettingsPanel.tsx: TD management — toggle, add/edit/delete/reorder, base64 logo upload
- DirectorPortal.tsx: Sponsors panel in mobile carousel + desktop grid
- App.tsx: intercept "Begin Playing" → fetch sponsors → show SponsorCardFlow before game
  - Fixed (s: any) cast → properly typed as { isActive: boolean }

## Round-robin regression fixes
- DrawDialog.tsx: revert to user-drawn par selection (1–5 card values);
  remove onSelectStartingHole prop and HOLE_PARS auto-assignment
- GameScreen.tsx: effect now always shows DrawDialog for par draw (no auto-assign);
  remove undefined game.updateStartingHole reference; remove HOLE_PARS import/usage;
  par display shows drawn value or "—" when not yet set; mercy button only shows
  when par is confirmed
- constants.ts: remove HOLE_PARS (fixed-par-per-hole array was out of scope)
- PlayerSetup.tsx: add Starting Hole selector (Hole 1–18 dropdown) for local games;
  Start button passes selected hole to onStartGame(startingHole)
- SummaryScreen.tsx: show holesCompleted/18 format in leaderboard rows and
  player detail dialog
